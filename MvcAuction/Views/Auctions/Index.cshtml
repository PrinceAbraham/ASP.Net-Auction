@model IEnumerable<MvcAuction.Models.Auction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@foreach (var item in Model)
{
    //Without item it will use Model as the parameter
    @Html.Partial("_AuctionTile", item);
}
